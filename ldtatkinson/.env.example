# The IP address and port the server will bind to.
# This value is read by the Axum server during startup.
LEPTOS_SITE_ADDR="127.0.0.1:3000"

# The port used by the live-reload websocket.
# This is used by the client to automatically reload when the server restarts.
LEPTOS_RELOAD_PORT="3001"

# The log level for the application.
# Valid values are: trace, debug, info, warn, error
RUST_LOG="info"
